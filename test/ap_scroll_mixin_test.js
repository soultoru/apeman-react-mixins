/**
 * Test case for apScrollMixin.
 * Runs with mocha.
 */
"use strict";

const apScrollMixin = require('../lib/ap_scroll_mixin.js'),
    assert = require('assert');

describe('ap-scroll-mixin', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap scroll mixin', (done) => {
        done();
    });
});

