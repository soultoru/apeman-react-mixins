/**
 * Test case for events.
 * Runs with mocha.
 */
"use strict";

const events = require('../lib/events.js'),
    assert = require('assert');

describe('events', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Events', (done) => {
        done();
    });
});

