/**
 * Test case for apFlushMixin.
 * Runs with mocha.
 */
"use strict";

const apFlushMixin = require('../lib/ap_flush_mixin.js'),
    assert = require('assert');

describe('ap-flush-mixin', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap flush mixin', (done) => {
        done();
    });
});

